# Simple Calculator build with ReactJS

The features that i implement here are:

- add, subtract, multiply, divide
- support decimal values
- reset functionality
- format larger numbers
- output resize based on length
